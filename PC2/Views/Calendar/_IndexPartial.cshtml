@using PC2.Models

@{
    // Receive the event description
    string eventDescription = ViewData["eventDescription"]?.ToString() ?? string.Empty;

    // Convert URLs, emails, and phone numbers to clickable links
    string eventDescriptionResult = TextLinkifier.Linkify(eventDescription);

    // Display the event description with links
    @Html.Raw(eventDescriptionResult)
}
